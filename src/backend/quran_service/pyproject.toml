[project]
name = "quran_service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100",
    "uvicorn>=0.23",
    "shared",
    "rapidfuzz>=3.0.0",
    "regex>=2023.0.0",
    "pytest>=7.0.0",
    "httpx>=0.24.0",
]
