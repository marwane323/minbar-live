[project]
name = "asr_service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100",
    "uvicorn>=0.23",
    "shared",
    "faster-whisper",
    "numpy",
    "websockets",
    "soundfile",
    "scipy",
    "pytest",
    "httpx",
]
