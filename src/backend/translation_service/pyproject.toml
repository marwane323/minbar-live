[project]
name = "translation_service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.100",
    "uvicorn>=0.23",
    "shared",
    "httpx>=0.24.1",
    "tiktoken>=0.4.0",
    "pydantic>=2.3.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.1"
]
